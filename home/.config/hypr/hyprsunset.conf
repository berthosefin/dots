# Optional: set max gamma limit if controlling via hyprctl
max-gamma = 150

# Daytime profile: normal color and brightness
profile {
    time = 07:00
    identity = true
}

# Evening/Night profile: warm blue-light filter
profile {
    time = 19:00
    temperature = 4500
    gamma = 0.9
}
